# 多终端云同步 — 交付说明

## 一句话

给听写工具加了「云端同步」：词库存进你自己的 GitHub 仓库，**A 机改完点一下上传，B/C 机打开就自动同步**，不用再手动导出→传输→导入。

---

## 你需要做的（一次性）

### 1. 建一个存数据的仓库
GitHub 上新建一个仓库（比如 `dictation-data`），**公开或私有都行**。
- 建议：就用现在放 app 的那个仓库，数据文件直接放进去，少一个仓库要管。
- 也可以新建一个专用仓库，随便。

> 仓库建好后什么都不用建文件，程序第一次上传会自己创建。

### 2. 申请一个 Token
GitHub → 右上角头像 → **Settings** → 左侧最下面 **Developer settings** → **Personal access tokens** → **Tokens (classic)** → **Generate new token (classic)**

- **Note**：随便写，比如 `听写工具同步`
- **Expiration**：建议选 `No expiration`（否则过期后要重新配）
- **Scopes**（权限勾选）：
  - 仓库是**公开**的 → 只勾 **`public_repo`**
  - 仓库是**私有**的 → 勾 **`repo`**（整个）
- 点 **Generate token**，复制那串 `ghp_...`

> ⚠️ **Token 只显示一次**，复制好。忘了就重新生成一个。
> 💰 Token 免费，没有后续费用，也不会自动扣钱。
> 🔒 Token 只存在你本机浏览器里，不会上传到任何地方。

### 3. 在每台设备上填一次
打开听写工具 → 「💾 数据管理」→「☁️ 云端同步」→「⚙️ 同步设置」：

| 字段 | 填什么 |
|---|---|
| GitHub 用户名 | 你的用户名（或组织名） |
| 仓库名 | 比如 `dictation-data` |
| Personal Access Token | 刚才复制的 `ghp_...` |
| 数据文件路径 | 默认 `data/words.json`，不用改 |

点 **保存并测试** → 提示「连接成功」就成了。

### 4. 第一次上传
在**你数据最全的那台设备**上点「⬆️ 上传到云端」。

> 如果云端已有数据，会弹确认框 —— 看清提示再点确定，上传是**用本机覆盖云端**。

之后其他设备打开工具，1~2 秒后会自己同步，弹一句「☁️ 已从云端同步：新增 X 个词」。

---

## 日常怎么用

```
【A 机】改词库/加词
   ↓
【A 机】点「⬆️ 上传到云端」
   ↓
【B 机 / C 机】打开工具 → 自动同步（什么都不用点）
```

- 想手动拉一次 → 点「⬇️ 从云端同步」（**合并**，谁都不得丢）
- 想大力覆盖 → 「同步设置」里的「⚠️ 强制对齐云端」（**用云端覆盖本机**，会二次确认）
- 手滑了 → 「↩️ 撤销上次同步」（恢复到同步前）

---

## 几个关键设计（都是你确认过的）

### 同步是「合并」，不是「覆盖」
只要点的是「从云端同步」和自动同步，**永远不会丢词**：

| 情况 | 结果 |
|---|---|
| 云端有新词，本机没有 | 加进来 |
| 本机有词，云端没有 | **留着**（绝不丢） |
| 云端标了删除 | 本机也删 |
| 本机删了、云端还有 | 不会又跑回来 |
| 同一个词两边都改过 | 取**改得晚**的那份 |

### 删除靠「墓碑」记录
每删一个词，就记一笔「这个词在 X 时间被删了」。
否则 A 机删了，B 机同步时一看「本机有、云端没有」，又会把词塞回云端 —— 删不干净。
墓碑在词条重新加回来后会自动作废，不会越积越多。

### 错词本也同步，「设置」不同步
- 错词本、错词次数 → 同步
- 听写范围/顺序/语速/显示选项这些**偏好设置 → 每台设备各管各的**（手机和电脑习惯不一样，不强行统一）

### 小词库按「名字」认，不按编号
所以两台设备各自建的「第一单元」，会被认成同一本，里面的词做并集，不会变成两本同名副本。

### 顺手修的两个老毛病
1. **编辑释义不同步到小词库** —— 以前在小词库的副本里改不了，现在主词库改了会自动回填所有小词库副本。
2. **页面刷新后 id 会从 1 重算** —— 以前新导入的词可能和已有词撞 id，现在不会了。
3. 另修了 4 处词条内容未转义的地方（防注入）。

---

## 本地备份/恢复还能用吗？

**能，而且建议继续用。** 两者不冲突：

- **云同步**：日常增量同步，省事，但依赖网络 + Token 有效
- **本地备份**：一份完整的离线保命文件。Token 过期、误删、想整体搬到新设备，都能兜住

建议：**大改动前顺手点一次「💾 备份数据」**。

---

## 一个提醒

仓库要是设为**公开**，任何人都能读到你的词库内容（只能读，改不了）。
你说过无所谓 —— 那就公开，Token 权限也能少勾一点。

---

## 改了什么文件

| 文件 | 改动 |
|---|---|
| `data.js` | 合并逻辑重写（按词不按 id）、墓碑、`SyncManager`（含 GitHub 读写与校验） |
| `app.js` | 墓碑记录接入所有删除入口、`nextId` 修正、小词库副本回填、云同步主流程 |
| `ui.js` | 云同步界面 8 个函数、4 处转义修补、导入流程读写墓碑 |
| `index.html` | 数据管理区新增「☁️ 云端同步」入口 + 设置弹窗 |
| `service-worker.js` | 缓存版本 v63 → **v64** |
| `data/words.json` | 新建，云端数据模板 |

**自测**：7 个 JS 语法全绿；新写同步单元测试 **58/58 通过**；既有回归 **29/29 + 27/27 + 42/42** 全绿；HTML 43 个按钮的函数引用 0 缺失。

---

## 还没做的

**真机实测**。目前是代码完成 + 本地自测通过，还没用真实 GitHub Token 跑过一轮端到端同步。
你配好之后，建议按这个顺序验一次：

1. A 机「同步设置」→ 保存并测试 → 应提示连接成功
2. A 机「上传到云端」→ 去 GitHub 仓库看，应该多了 `data/words.json` 且里面有你的词
3. B 机配好同样的参数 → 刷新页面 → 应在 1~2 秒内弹出同步提示，词库齐了
4. B 机加一个词再上传 → A 机刷新 → 该词应出现
5. B 机删一个词再上传 → A 机刷新 → 该词应消失（墓碑生效）

哪一步不对，把提示文字发我。
